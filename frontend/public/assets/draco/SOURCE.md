# Draco Decoder Source

These decoder files were copied from the `three` package:

`node_modules/three/examples/jsm/libs/draco/gltf`

They are used locally by `DRACOLoader` to decode the NASA Space Shuttle GLB model.
